// Define a string of JavaScript code with a function
let code = `
function greet(name) {
  console.log("Hello, " + name + "!");
}
`;

// Evaluate the code
eval(code);

// Call the function defined in the code
greet("John"); // Output: "Hello, John!"
